# Translations

Laravel translation files (PHP arrays or JSON). Examples:

- `th.json` — Thai translations
- `en.json` — English translations

Configure via `config/app.php`:
```php
'locale' => 'th',
'fallback_locale' => 'en',
'lang_path' => base_path('app/lang'),
```
